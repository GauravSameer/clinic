server:
  port: 8081
spring:
  application:
    name: clinic_application
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cms
    username: root
    password: test123
  jpa:
    hibernate:
      ddl-auto: update
